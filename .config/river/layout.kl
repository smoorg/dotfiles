const masterV ( Vertical | Horizontal Horizontal )
const stackingV (Vertical | Deck Deck)

const masterH ( Horizontal | Vertical Vertical )
const stackingH (Horizontal | Deck Deck)
const tab Deck

const left ( 3 > stackingV ? masterV )
const right ( [ - 1 - ] left )

const top ( 3 > stackingH ? masterH )
const bottom ([ - 1 - ] top)

const default left
